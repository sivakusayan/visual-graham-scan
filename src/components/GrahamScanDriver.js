/**
 * Serves as the component that allows the user to interact
 * with the graham scan algorithm.
 */

import React from 'react';

const GrahamScanDriver = () => null;

export default GrahamScanDriver;

/**
 * Layer containing any points which are currently
 * accepted by the algorithm to possibly be on the
 * convex hull.
 */

import React from 'react';
import { Layer, Circle } from 'react-konva';
import POINT_CONFIG from '../../constants/POINT_CONFIG';

const AcceptedPointLayer = ({ points }) => (

);

export default AcceptedPointLayer;
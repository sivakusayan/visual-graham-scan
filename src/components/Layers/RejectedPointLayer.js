/**
 * Layer containing any points which were rejected
 * from the convex hull.
 */

import React from 'react';
import { Circle } from 'react-konva';

const RejectedPointLayer = ({ points }) => (

);

export default RejectedPointLayer;